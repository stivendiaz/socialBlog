<!DOCTYPE html>
<html>
<head>
  <title>SuperBlog</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'icon', :type => "image/x-icon" %>
</head>

<body>

  <nav class="navbar bg-primary navbar-dark navbar-expand-sm border-bottom box-shadow">
    <%= link_to "SuperBlog", root_path, class: "navbar-brand " %>


    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse " id="navbarSupportedContent">

      <ul class="navbar-nav ml-auto ">
        <% if user_signed_in? %>
        <li class="nav-link"><%=current_user.email%></li>
        <li class="nav-item">
          <%= link_to "Salir", destroy_user_session_path, method: "delete", :class => "nav-link"  %>
        </li>

          <% else %>
          <li class="nav-item">
            <%= link_to "Ingresar", new_user_session_path, :class => "nav-link"  %>
          </li>
          <li class="nav-item ">
            <%= link_to "Regístrarse", new_user_registration_path, :class => "btn btn-light  "  %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>

  <div class="container" style="margin-top:30px">
    <%= yield %>
  </div>
</body>
</html>


<!--<div class="text-center">

  <div class="border-form-div">
    <h2>Registrarse</h2>
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <%= f.email_field :email, :autofocus => true, :placeholder => 'Email' %>
      <%= f.password_field :password, :placeholder => 'Contraseña' %>
      <%= f.password_field :password_confirmation, :placeholder => 'Confirmar Contraseña' %>
      <%= f.submit "Registrarse",:class=>'btn btn-outline-dark' %>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>

-->


<div class="alert-danger"><%= devise_error_messages! %></div> 
<div class="container">  
  <div class="row">
    <div class="card col-md-4 mx-auto p-0 bg-light">
      <div class="card-header text-primary h4"><span class="ion-person-add"></span> Sign up</div>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
   

      <div class="card-body">
        <div class="form-group">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="form-group">
          <em><%= f.label "password, mínimo de 6 carácteres" %></em>  
          <% if @minimum_password_length %>

          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Confirma Password"%>
        </div>
        <br>

        <div class="actions">
          <%= f.submit "Sign up", class: "btn btn-primary  btn-block" %>
          <%= render "devise/shared/links"%>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>